name: Test

on:
  push:
    branches-ignore:
      - wheels
  pull_request:
    branches:
      - master
      - dev

jobs:
  test:
    name: Test (${{ matrix.os }}, py${{ matrix.python-version }}, tf${{ matrix.tensorflow }})
    runs-on: ${{ matrix.os }}
    env:
        SETUPTOOLS_USE_DISTUTILS: stdlib
    strategy:
      fail-fast: false
      matrix:
        # os: [ubuntu-20.04, windows-latest, macos-latest]
        # python-version: ['3.6', '3.7', '3.8', '3.9', '3.10']
        # os: [windows-latest, windows-2019]
        os: [windows-latest]
        python-version: ['3.10']
        tensorflow: [1, 2]
        exclude:
            # https://www.tensorflow.org/install/pip#system-requirements
          - python-version: '3.8'
            tensorflow: 1
          - python-version: '3.9'
            tensorflow: 1
          - python-version: '3.10'
            tensorflow: 1
          - os: windows-latest
            tensorflow: 1

    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pip wheel setuptools
      # - run: pip install "tensorflow==${{ matrix.tensorflow }}.*"
      # - uses: actions/checkout@v3

      # - if: matrix.tensorflow == 1
      #   run: echo "package_extras=test,tf1" >> $GITHUB_ENV
      #   shell: bash
      # - if: matrix.tensorflow == 2
      #   run: echo "package_extras=test" >> $GITHUB_ENV
      #   shell: bash

      # - name: Install edt
      #   if: matrix.python-version == 3.9
      #   run: pip install edt "numpy>=1.20"

      - name: foo
        run: |
          mkdir /tmp1
          mkdir /tmp2
          mkdir /tmp3
        shell: bash

      # - run: pip download --dest . --no-binary stardist stardist
      # - run: pip install -v ./stardist-0.8.3.tar.gz
      #   # env:
      #   #   TEMP: "/d/a/stardist/stardist/tmp"
      #   #   TMPDIR: "/d/a/stardist/stardist/tmp"
      #   #   TMP: "/d/a/stardist/stardist/tmp"
      # - run: pip uninstall --yes stardist

      # - uses: ilammy/msvc-dev-cmd@v1

      - run: pip install -v --no-binary stardist stardist
        env:
          TEMP: "/tmp1"
          # TMPDIR: "/tmp2"
          TMP: "/tmp3" # <-- only this is used on windows



      # - name: build wheel
      #   run: pip install -v --config-settings "bdist-dir=/d/a/stardist/stardist/tmp" --no-binary stardist stardist

      # - name: Install package from source (just to test wheel building)
      #   run: pip install --no-binary stardist "stardist[${{env.package_extras}}]"